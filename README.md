# Jwt_Authentication_module
JWT-based authentication module built with Spring Security, OAuth2, and Keycloak as the identity provider. It provides secure API protection through token validation, role-based access control, and seamless integration with centralized authentication and authorization services.
